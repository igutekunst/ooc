/*
 *
 *
 *
 * Ideas for iteration
 *
 * From Python
 *
 *  iter(object *) returns an iterator instance
 *
 *
 *  next(iterator) returns an object, or NULL if the last item has reached
 *
 * Usage
 *      void * iter = __iter(iterable)
 *      for(void * i = next(iter); i != NULL ; i = next(iter) {
 *      
 *      }
 */
